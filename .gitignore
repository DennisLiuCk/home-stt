# --- Project runtime ---
# PID file produced by stt-start.ps1
stt-daemon.pid
# Logs (we write to %TEMP% by default, but local runs may dump these here)
*.log

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
*.egg
pip-log.txt

# --- OS metadata ---
.DS_Store
Thumbs.db
desktop.ini

# --- Editors / IDEs ---
.vscode/
.idea/
*.swp
*~
*.sublime-workspace

# --- Local secrets / environment ---
.env
.env.*
!.env.example

# --- Editor / agent integrations ---
# Claude Code TTS-summary scratch file (Stop hook writes / reads / deletes
# this from ~/.claude; if a hook misfires and drops it here instead, don't
# accidentally commit transcript snippets).
tts-summary-now.txt

# --- Spike / scratch ---
# v0.8.0 encoder-pipelining spike (throwaway A/B scripts that load real
# models). Per plan-v0.8.0 Day 1-2 gate, this should NEVER be committed —
# production code goes in scripts/.
tmp/
